galaxy_info:
  author: Peter Galonza
  description: Install docker.

  license: Apache-2.0

  min_ansible_version: 2.9.10

  galaxy_tags:
    - linux
    - docker

dependencies:
  - role: geerlingguy.docker
    vars:
      docker_users:
        - "{{ ansible_user }}"
