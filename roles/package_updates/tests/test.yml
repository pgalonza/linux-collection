---
# test.yml for package_updates role - simplified version

- name: Test security updates with pinning
  hosts: localhost
  remote_user: root
  gather_facts: false
  vars:
    package_updates_mode: "security"
    package_pin_list:
      - python3

