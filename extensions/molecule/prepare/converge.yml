---
- name: Converge
  hosts: all
  gather_facts: false
  tasks:
    - name: Testing role
      ansible.builtin.include_role:
        name: pgalonza.linux.prepare
        tasks_from: main.yml
      vars:
        ansible_user: ssh_user
        ssh_key_dir: /tmp
