---
- name: Cleanup
  hosts: all
  gather_facts: false
  tasks:
    - name: Remove audit directory and its contents
      become: false
      ansible.builtin.file:
        path: "./audit"
        state: absent
      delegate_to: localhost
      run_once: true
